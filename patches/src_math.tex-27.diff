--- a/src/math.tex
+++ b/src/math.tex
@@ -537,8 +537,8 @@ work.\footnote{The \AmS-\LaTeX{} package makes the \ci{textrm} command
 \end{equation}
 \end{example}
 
-Nevertheless, sometimes you need to tell \LaTeX{} the correct font
-size. In math mode, the fontsize is set with the four commands:
+Sometimes you still need to tell \LaTeX{} the correct font
+size. In math mode, this is set with the following four commands:
 \begin{flushleft}
 \ci{displaystyle}~($\displaystyle 123$),
  \ci{textstyle}~($\textstyle 123$), 
