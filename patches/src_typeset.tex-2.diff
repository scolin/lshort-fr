--- a/src/typeset.tex
+++ b/src/typeset.tex
@@ -530,8 +530,9 @@ DOS, OS/2  &  \iei{cp850} & \iei{cp866nav}
 \end{tabular}                
 \end{center}                 
 
-If you use multilingual document with conflicting input encodings, 
-you might want to switch to unicode with the help of \pai{ucs} package. 
+If you have a multilingual document with conflicting input encodings,
+you might want to switch to unicode, using the \pai{ucs} package.
+
 
 \begin{lscommand}
 \ci{usepackage}\verb|{ucs}|\\ 
