--- a/src/math.tex
+++ b/src/math.tex
@@ -440,11 +440,11 @@ sign. The \verb|\\| command breaks the lines. If you only want to enumerate some
 of equations, use \ci{nonumber} to remove the number. It has to be placed
 \emph{before} \verb|\\|:
 \begin{example}
- \begin{align}
-  f(x) &= (a+b)(a-b) \label{1}\\
-       &= a^2-ab+ba-b^2  \\ 
-       &= a^2+b^2 \tag{wrong}
- \end{align}
+\begin{align}
+f(x) &= (a+b)(a-b) \label{1}\\
+     &= a^2-ab+ba-b^2  \\ 
+     &= a^2+b^2 \tag{wrong}
+\end{align}
 This is a reference to \eqref{1}.
 \end{example}
 
