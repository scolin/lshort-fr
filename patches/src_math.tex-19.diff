--- a/src/math.tex
+++ b/src/math.tex
@@ -412,7 +412,7 @@ x_{21} & x_{22} & \ldots \\
 \end{displaymath}
 \end{example}
 
-The \ei{array} environment can also be used to typeset expressions which have one
+The \ei{array} environment can also be used to typeset expressions that have one
 big delimiter by using a ``\verb|.|'' as an invisible \ci{right} 
 delimiter:
 \begin{example}
