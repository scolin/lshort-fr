--- a/src/lssym.tex
+++ b/src/lssym.tex
@@ -109,7 +109,7 @@ You can negate the following symbols by prefixing them with a \ci{not} command.
 
 
 \begin{table}[!tbp]
-\caption{Arrows.}
+\caption{Arrows.} \label{tab:arrows}
 \begin{symbols}{*2{cl}}
  \X{\leftarrow}or \verb|\gets|& \X{\longleftarrow} \\
  \X{\rightarrow}or \verb|\to|& \X{\longrightarrow} \\
