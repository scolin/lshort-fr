--- a/src/math.tex
+++ b/src/math.tex
@@ -247,6 +247,10 @@ For the \wi{modulo function}, there are two commands: \ci{bmod} for the
 binary operator ``$a \bmod b$'' and \ci{pmod}
 for expressions
 such as ``$x\equiv a \pmod{b}$.''
+\begin{example}
+$a\bmod b$\\
+$x\equiv a \pmod{b}$
+\end{example}
 
 A built-up \textbf{\wi{fraction}} is typeset with the
 \ci{frac}\verb|{...}{...}| command.
