--- a/src/typeset.tex
+++ b/src/typeset.tex
@@ -525,7 +525,7 @@ To enable hyphenation and change all automatic text to \wi{Portuguese},
 \begin{lscommand}
 \verb|\usepackage[portuguese]{babel}|
 \end{lscommand}
-Or if you are in Brazil, substitute the language for \wi{brazilian}.
+Or if you are in Brazil, substitute the language for \texttt{\wi{brazilian}}.
 
 As there are a lot of accents in Portuguese you might want to use
 \begin{lscommand}
