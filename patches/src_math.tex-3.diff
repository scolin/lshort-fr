--- a/src/math.tex
+++ b/src/math.tex
@@ -426,7 +426,7 @@ y = \left\{ \begin{array}{ll}
 \end{example}
 
 As within the \verb|tabular| environment you can also
-draw lines in the \ei{array} environent, e.g. separating the entries of
+draw lines in the \ei{array} environment, e.g. separating the entries of
 a matrix:
 \begin{example}
 \begin{displaymath}
